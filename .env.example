# Optional: same-origin base paths that proxy to cloud LLM APIs (needed when hosting the static
# build without CORS, e.g. nginx /api/openai → https://api.openai.com).
# VITE_OPENAI_API_BASE=/api/openai
# VITE_ANTHROPIC_API_BASE=/api/anthropic
# VITE_GEMINI_API_BASE=/api/gemini
